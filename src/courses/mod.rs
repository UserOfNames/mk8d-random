//! Module defining the `CourseList` struct that holds state pertaining to a given course list, as
//! well as the `Course` structs defining a specific course.

pub mod course;
pub mod course_list;
mod history;
